public class Identifiers {
    public static void main(String[] args) {
        // Identifier "salary"
        // Identifiers can't start with a number
        // Only can start with $, _, etc, avoid use special characters
        double salary = 100000.45;
    }
}
